spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/chatapp_application
    username: root
    password: qkrwnsghd
  jpa:
    hibernate:
      ddl-auto: create
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    
logging:
  level:
    org:
      springframework:
        security: DEBUG
        test:
          context:
            cache: DEBUG
        
jwt:
  secret: makeItVerySecret
  issuer: JunPark